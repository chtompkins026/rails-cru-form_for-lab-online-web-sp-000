<% @songs.each do |song| %>
  <p><%= link_to song.name, song_path(song) %></p>
<% end %><% @songs.each do |song| %>
  <p><%= link_to song.name, song_path(song) %></p>
<% end %><% @songs.each do |song| %>
  <p><%= link_to song.name, song_path(song) %></p>
<% end %><% @songs.each do |song| %>
  <p><%= link_to song.name, song_path(song) %></p>
<% end %>